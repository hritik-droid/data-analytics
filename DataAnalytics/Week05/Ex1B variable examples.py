# Exercise 1.B - Variable Examples

customer_id = 1001 # I used a number, but some companies may use letters too.
customer_name = "Alex Johnson" # I used first and last name together, but they could be separate.
customer_gender = "Female" # This is a string because gender is written as text.
customer_date_of_birth = "1998-04-15" # I used a string so the date format stays clear.
drivers_license_number = "D1234567" # I used a string because license numbers can include letters.
auto_policy_number = "AP987654" # I used a string because policy numbers may include letters.

my_name = "Hritik"
hometown = "San Francisco, California"


# Python reserved words:
# False, None, True, and, as, assert, async, await, break, class,
# continue, def, del, elif, else, except, finally, for, from,
# global, if, import, in, is, lambda, nonlocal, not, or, pass,
# raise, return, try, while, with, yield

# if: Used to run code only when a condition is true.
# else: Used when the if condition is false.
# for: Used to repeat code while looping through items.
# while: Used to repeat code as long as a condition stays true.
# return: Used inside a function to send a value back.